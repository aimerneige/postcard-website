# 验证记录

日期：2026-09-15。环境：Node.js 22.22.2，Chromium 153（Playwright，无头浏览器）。

## 结果

- `npm ci`：成功，审计 0 漏洞。
- 从缺少字体、测试图片和 `dist` 的状态运行准备脚本，再执行构建与测试，验证可重新生成所有二进制资源。
- `npm run build`：TypeScript 检查及 Vite 生产构建通过，无构建警告。
- `npm test`：19 项测试全部通过。
- `npm run test:e2e`：11 项浏览器测试全部通过。
- 桌面 1280px、移动 390px / 375px 布局检查通过，无横向溢出；已查看桌面与移动截图。
- 默认无图片、禁用导出、左右文案默认留空、字体就绪状态正常。
- 实际 PNG 下载验证为横版 1748 × 1240px、竖版 1240 × 1748px，pHYs 密度单测通过。
- 实际 PDF 下载可被 pdf-lib 重新读取，为单页，MediaBox 分别为横版 148 × 105 mm、竖版 105 × 148 mm。
- JPEG 8 种 EXIF 方向的归一化尺寸通过浏览器验证。
- 透明 PNG 的实际预览采样为不透明白色。
- 缩放、旋转、恢复、方向键微调、换图保留文案、窗口变化保持 PPI 通过。
- 动画 PNG / WebP、伪造 JPEG、多文件拖入、超过 30 MiB 文件均被拒绝；此前图片与构图保留。
- 40 MP 预检通过单测。
- 横竖切换会恢复构图、保留文字与样式；竖版 cover、旋转后的 PPI 与空预览比例通过验证。
- 以纯白不透明白条导出两种方向的 PNG，对底部像素逐行采样，两者白条均约 76px（约 6.4 mm），未随竖版高度拉伸。竖版在根路径、仓库子路径和手机布局均通过。
- 同一构建在 `/` 与 `/postcard/` 加载并成功导出 PDF；包含相对路径延迟模块。
- 浏览器流程无 pageerror、无第三方请求。
- 默认日语优化字体 irohamaru 与最初附件保持相同 SHA-256；新增六款字体使用固定上游提交的原始文件，来源及 SHA-256 在 `licenses/font-sources.json`。OFL 与依赖许可随构建分发。
- 七种字体在根路径和仓库子路径均能选用；每种字体导出的 PNG 与 PDF 内嵌图像逐像素一致，七种中英日混排文字渲染结果互不相同。
- 新增 Lato、Lora、Caveat 三款英文字体；初始不请求未选字体；切换不改变文字、缩放和不透明度；手机布局无横向溢出。
- 字体加载期间禁止导出；过期字体请求不会覆盖当前选择；模拟字体请求失败后可重试恢复。字体许可链接在两种路径均可读取。
- 上传真实 TTF 后，IndexedDB 中的字体 Blob 校验值与原文件一致；刷新、仓库子路径及同浏览器的新标签页均可恢复。上传字体可用于 PNG/PDF，逐款删除后缓存与默认选择同步更新。
- 损坏、伪造、超过 20 MiB 的字体文件被拒绝，并保留当前文字与字体选择。浏览器流程无外部字体上传请求。
- `dist` 没有 JPEG、原附件或测试文件，没有 `.github/workflows`。

七款字体的校验值与仓库文件一致，字体作为唯一二进制例外提交；测试图像按文档生成并被 Git 忽略。截图与下载样本位于 `.local/`；原始动画画面仅曾用于本地复现检查。自动化测试使用自生成图片。

## 实测边界

未执行外部部署、DNS 操作、真机 Safari / Firefox 验证、实际打印或 40 MP 边界内存压力测试。Canvas 原始 CSS 行高和 `locl` 的差异见 README，不能将字体文件一致理解为原网页所有排版逐像素一致。
